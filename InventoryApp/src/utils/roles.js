export const isAdminRole = (role) => role === "admin" || role === "superadmin";

export const isSuperAdminRole = (role) => role === "superadmin";

export const isCheckerRole = (role) => role === "checker";
